#pragma once
#include <vector>
class Level{
	private:
		std::vector<std::vector <int>> groundLevelTiles;
		std::
	public:
		void buildGround(int X, int Y, int iWidth, int iHeight);
		// void structGND2(int X, int Y, int iSize, bool bDir); // true = LEFT, false = RIGHT
		// void structGND2(int X, int Y, int iWidth, int iHeight);
};